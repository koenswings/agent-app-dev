# Node
node_modules/
dist/

# App repos cloned for work
apps/app-kolibri/
apps/app-kiwix/
apps/app-nextcloud/
apps/app-kolibri-studio/
apps/app-seafile/

# varia — separate repos, not tracked here
varia/baby-milk-tracker/
varia/milkwise/

# app-milkwise is a separate repo
apps/app-milkwise/

# App repos nested at the workspace root on the Pi
# (/home/pi/idea/agents/agent-app-dev/app-*) — separate repos, not tracked here
/app-*/

# Expo/React Native secrets
varia/milkwise/AuthKey_*.p8
varia/milkwise/.env.local
varia/baby-milk-tracker/.env.local

# Temp / generated
tmp/
*.log

# Standalone product repos (not committed in agent-app-dev)
varia/
