# Daily Notes — 2026-06-17

## Session overview

Koen ran `/flush` and `/init` at 20:07 Brussels time.

---

## Heartbeat status

- Heartbeat with `AUTH_TOKEN` returns 401 Unauthorized — token appears stale/expired
- Heartbeat with `MC_PLATFORM_TOKEN` works fine → used that as workaround
- Need to check/refresh AUTH_TOKEN in `.env` (may have rotated)

---

## MC board state (as of this session)

Two tasks on the App Dev board:

| Status | Task |
|--------|------|
| review | Build app-milkwise: package Baby Milk Tracker as IDEA App Disk |
| review | Baby Milk Tracker — Full Web App |

No in_progress tasks.

---

## Work in progress: §3.5 + §3.6 feature

A Claude Code sub-session was running (nova-blo killed by SIGKILL at 12:09) implementing:
- **§3.5: Next Bottle Selector** — persistent `nextBottleWaterMl` setting, pill + picker on dashboard
- **§3.6: Log Feed pre-fill** — `?recommend=` query param on /log page

### Current state of uncommitted changes

**`varia/baby-milk-tracker`** (8 files modified + `.claude-task.md` untracked):
- `src/types/index.ts` — added `nextBottleWaterMl: number` to Settings
- `src/lib/store.ts` — added `nextBottleWaterMl: 90` to DEFAULT_SETTINGS
- `src/lib/calculations.ts` — exported `smoothedAtTime`, updated `findTargetAwareNext` signature, updated `nextFeedTime` to use `nextBottleWaterMl`, added `bestBottleSizeNow()`
- `src/app/page.tsx` — state for both pickers, modal UIs, standard/P3 bottle picker pills, live smoothed gauge
- `src/app/settings/page.tsx` — minor
- `src/app/log/page.tsx` — `?recommend=` pre-fill + Suspense
- `src/components/cards/StatusCard.tsx` — liveSmoothedMl/Pct props
- `src/lib/server-store.ts` — minor

**`apps/app-milkwise`** (same 9 files including `compose.yaml`) — same changes mirrored plus compose.yaml edit

The work is **substantially done** (all diffs visible in git status), but NOT committed. The coding agent was killed mid-session. Need to:
1. Verify the implementation is complete (check remaining unshown diffs)
2. Commit in both repos
3. Build + deploy to idea02

### `.claude-task.md`
Task spec file in `varia/baby-milk-tracker/` — describes §3.5 + §3.6 in full. Should be deleted after work is committed.

---

## apps/ dir contents

- `apps/app-milkwise/` — cloned, has uncommitted changes
- `apps/app-harness/` — present

---

## varia/ dir contents

- `varia/baby-milk-tracker/` — Next.js web app (IDEA version), uncommitted changes
- `varia/milkwise/` — React Native app, clean (last commit: ef8e373 sync Predictor 3)

---

## baby-milk-tracker version

Last committed version: **1.0.64** (2026-06-10)
Pending: §3.5 + §3.6 changes not yet committed

---

## v1.0.65 — SHIPPED ✅ (20:50 Brussels)

### What was found and fixed
Orphan sequence 1.0.66–1.0.74 existed on idea02 from June 13 (9 untracked builds). Key feature from 1.0.74 that would have been lost:
- **Live gauge in StatusCard**: `PanelWithGaugeLive` + `PanelDualGauge` — smoothed value decays in real time with 60s tick
- Previous agent had stripped `livePct` prop from `PanelWithGauge` (dead in 1.0.74 too, but broke the type)
- `SmoothedExplainer now=` was pointing to live clock instead of `smoothedAt` (wrong freeze point)

### Changes in v1.0.65
- §3.5: Next bottle selector pill + picker for Standard and P3 predictors
- §3.6: Log feed pre-fill via `?recommend=X` query param with ✓ badge
- Smoothed explainer: bottle credit table (frozen at lastFeed.timestamp)
- Live gauge feature from orphan builds preserved
- TypeScript clean, Next.js build clean

### Deploy
- Both repos pushed (force-with-lease on amended commits)
- Image: `koenswings/milkwise:1.0.65` + `:latest` on DockerHub
- idea02 `/instances/milkwise-idea02-001/compose.yaml` updated (1.0.74 → 1.0.65)
- Container restarted, serving at http://idea02.tail2d60.ts.net:3333/ ✅

## Key reminder

AUTH_TOKEN in .env is broken (401). Use MC_PLATFORM_TOKEN for API calls until fixed.
