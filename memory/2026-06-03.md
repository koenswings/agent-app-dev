# Daily Notes — 2026-06-03

## Session context
Long working session with Koen on MilkWise. Previous session (08be79a6) was forcefully reset at 09:04 UTC by OpenClaw — confirmed `.reset.` suffix. Atlas notified.

## Workspace restructure (done)
- `projects/` renamed to `varia/` — non-IDEA side projects
- `apps/` created — IDEA app repos go here when working on them
- `design/` confirmed location for all docs/PDFs
- README.md written for agent-app-dev workspace
- MEMORY.md fully rewritten from kit-app-dev-agent-full.pdf design spec
- Three Kit design PDFs moved from Atlas's workspace to `design/`: kit-app-dev-agent-full.pdf, kit-compatibility-matrix.pdf, kit-data-storage.pdf

## App naming (decided)
- `baby-milk-tracker` = IDEA App version (Next.js web, Pi port 3333)
- `milkwise` = commercial iOS/Android version (React Native/Expo, was milkwise-rn)
- `app-milkwise` = IDEA App Disk wrapper repo
- GitHub: `koenswings/milkwise-rn` renamed to `koenswings/milkwise`

## app.yaml files written and pushed
All 5 IDEA app repos now have app.yaml:
- app-kolibri: custom build, Kolibri .whl monitor
- app-nextcloud: retag ×3 (nextcloud-app, mariadb, collabora/code)
- app-kiwix: retag (migrate from unofficial image flagged)
- app-kolibri-studio: custom + retag ×3
- app-seafile: retag + direct ×2 (flagged for migration)

## Cross-agent tasks created
- Axle board: `[From Kit] Feasibility: requires_engine_min check at disk-dock time` (ID: eec1507f)
- Atlas board: `[From Kit] Feasibility: has build-instance migration been raised with Axle?` (ID: 93706283)
- App Dev board: `Build app-milkwise IDEA App Disk` task (ID: 418389aa) — inbox, awaiting in_progress

## MilkWise feature work (2026-06-03)
### Shared core sync (types/calculations) — both repos always updated together

### Color thresholds (configurable, in Settings)
- `yellowThresholdPct` default 5, `redThresholdPct` default 10
- Symmetric: within ±yellow% = green, ±yellow–red% = yellow, beyond ±red% = red
- Stored in Settings, stamped on feeds, used in dashboard cards + analytics bars

### Per-feed target stamping
- `targetMlPerDay` added to Feed type
- Historical feeds migrated with current target (one-time)
- New feeds stamped at log time
- Analytics dailyTotals returns targetMl per day
- Bar colours use day-specific target — historically accurate

### Analytics bar chart (RN)
- Replaced react-native-chart-kit BarChart with custom TouchableOpacity bars
- Tap a bar → detail row shows date, ml, target, %
- Target line removed (can't shift for multi-target periods — misleading)
- Legend: 🟢 on track · 🟡 slightly off · 🔴 overfed/behind
- Today always grey (day not finished)

### Analytics bar chart (web)
- Today bar grey
- Tooltip dark background (#0f172a), transparent cursor overlay
- Target line removed (same reason as RN)
- Bar colors use barColorHex() with configured thresholds

### Edit feed (both apps)
- Can now edit both volume AND timestamp (not just volume)
- Web: datetime-local picker
- RN: separate date (YYYY-MM-DD) + time (HH:MM) text inputs with KeyboardAvoidingView

### Dev/prod data split (RN)
- In __DEV__ mode: fetches from Pi API (EXPO_PUBLIC_API_URL=http://192.168.0.231:3333)
- In production: AsyncStorage (Supabase in v1.1)
- .env.example added, .env gitignored

### Smoothed explainer (both)
- Full modal with per-feed credit breakdown table + running totals
- Energy balance rationale: "baby burns hourlyRate ml-equiv/hour; bottle from 30h ago has spent 6h×rate of energy"
- Thresholds table is dynamic (uses configured yellow/red thresholds)

### 1-minute auto-refresh (dashboard, both apps)
- Web: setInterval + focus listener
- RN: useFocusEffect + setInterval, clears on blur

## Web app server
- Runs as systemd user service: `baby-milk-tracker.service`
- start.js: copies .next/static to standalone dir, sets DATA_DIR, PORT=3333, HOSTNAME=0.0.0.0
- Service: `systemctl --user enable/start baby-milk-tracker`
- Survives reboots

## Pending issue (end of session)
- iPhone Safari shows ERR_SSL_PROTOCOL_ERROR on `idea.tail2d60.ts.net:3333`
  - Cause: Tailscale HTTPS is enabled for the hostname (port 443 has TLS) but port 3333 is plain HTTP
  - Safari on iOS upgrades http:// to https:// automatically for .ts.net domains
  - Fix needed: either serve on port 443 with TLS, or give Koen the LAN URL
  - LAN URL: http://192.168.0.231:3333 (Pi's local IP, works on same WiFi, no Tailscale needed)
  - Expo Go: also failing — needs diagnosis

## Design doc updates (done)
- milkwise-commercialisation.md: two-sided color warnings, energy balance rationale, privacy section corrected (free=local, pro=Supabase), Supabase household design
- milkwise-mobile-data-analysis.md: full household design (tables, join flow, offline sync, conflict resolution)
- PDFs regenerated

## Atlas response (received)
- MEMORY.md approved as-is
- Atlas updated own MEMORY, Axle's MEMORY, Marco's MEMORY with Kit's IDs
- PDFs moved to Kit's design/ folder
- Atlas acknowledged onboarding gap — lesson logged
